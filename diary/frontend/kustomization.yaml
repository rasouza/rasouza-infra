apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: frontend

namespace: diary

resources:
- deployment.yaml
- service.yaml
- ingress.yaml

images:
- name: rasouza/diary-frontend
  newTag: 1d0df36

configMapGenerator:
- files:
  - envs/default.conf
  name: nginx-config
- files:
  - envs/config.js
  name: env-config
