apiVersion: v1
kind: Service
metadata:
  labels:
    service: frontend
  name: frontend-svc
  namespace: diary
spec:
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  selector:
    app: frontend