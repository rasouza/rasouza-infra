apiVersion: v1
kind: Service
metadata:
  labels:
    app: users
    service: users
  name: users
  namespace: diary
spec:
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  selector:
    app: users